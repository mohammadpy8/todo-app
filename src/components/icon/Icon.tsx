import React from 'react';
import { IconProps,} from '../../types/Icons.type';

const Icon = ({children, svg, png} : IconProps ) => {

    return (
        <div>
            
        </div>
    );
};

export default Icon;