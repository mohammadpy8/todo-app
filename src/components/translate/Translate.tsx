import React from 'react';

import { AllType } from '../../types/Translate.type';

const Translate = ({toTranslate}: AllType) => {
    return (
        <div>
            
        </div>
    );
};

export default Translate;